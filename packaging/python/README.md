# tickerforge-spec-data (Python)

Python package containing the canonical TickerForge specification files.

## Install

```bash
pip install tickerforge-spec-data
```

## Usage

```python
from tickerforge_spec_data import get_spec_root, get_version

spec_root = get_spec_root()
print(spec_root)
print(get_version())
```
